{% extends 'company_base.html.twig' %}

{% block content %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'prodly.form.status.submit'|trans({}, 'form') }}" formnovalidate />
    </div>
    {{ form_end(form) }}

{% endblock %}